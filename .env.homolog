# Docker Hub
DOCKERHUB_USERNAME=pacbel2022

# MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=notanacional_homolog
MYSQL_USER=notanacional_user
MYSQL_PASSWORD=password

# Database URL (ajustar conforme necessário)
# DATABASE_URL=mysql://notanacional_user:password@host.docker.internal:3306/notanacional_homolog

# Outras variáveis específicas das apps
NEXTAUTH_SECRET=test_secret_homolog
NEXTAUTH_URL=http://localhost:3001

# Para API .NET
JWT_SECRET=test_jwt_secret_homolog
SMTP_HOST=smtp.example.com
SMTP_USER=user@example.com
SMTP_PASS=password

# FTP settings se necessário
FTP_HOST=ftp.example.com
FTP_USER=user
FTP_PASS=password

# Certificados A1 path (se usado na API)
CERT_PATH=/app/certs
